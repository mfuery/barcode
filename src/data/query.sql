select SKU sku,
`Shade Name` shade,
`Color Name` color,
`Parent Product name` name,
`Statement of ID` tagline,
`Original Price` price,
`Discounted Price` price_discounted,
`Product URL` url,
`Bestseller` bestseller,
`Product BoxComp Image` img_box,
`Product Component Image` img_comp,
`Product Thumbnail Image` img_thumb,
`Shade Swatch Image` img_shade,
`WWL Carousel Image 1` img_wwl,
`How To Apply` how_to,
`Tips Tricks Video URL` video_url,
UPC upc
FROM rosie_us_feed;

SELECT
`SKU` AS sku,
`Position` AS position,
`Shade Name` AS shade_name,
`Color Name` AS color_name,
`Shade Swatch Image` AS img_shade_swatch,
`Discounted Price` AS discounted_price,
`Parent Product Id` AS parent_product_id,
`Parent Product name` AS parent_product_name,
`Statement of ID` AS statement_of_id,
`Original Price` AS price,
`Product URL` AS product_url,
`Meta Keywords` AS meta_keywords,
`Base Product Keywords` AS keywords,
`"Shop by Dilemma" category` AS dilemma_category,
`Category Rank` AS category_rank,
`Bestseller` AS bestseller,
`Collection` AS collection,
`Cross Sell SKU` AS cross_sell_skus,
`Discontinued` AS discontinued,
`Product Component Image` AS img_comp,
`Product Component Image Alt Text` AS img_comp_alt_text,
`Product BoxComp Image` AS img_boxcomp,
`Product Thumbnail Image` AS img_thumb,
`Product Description Title` AS product_description_title,
`Product Description` AS product_description,
`Product Details` AS product_details,
`Ingredients` AS ingredients,
`WWL Carousel Image 1` AS img_wwl_carousel,
`How To Apply Title` AS how_to_apply_title,
`How To Apply` AS how_to_apply,
`Beauty tip` AS beauty_tip,
`How to apply model image` AS img_hta_model,
`How to apply model image alt text` AS img_hta_model_alt_text,
`Tips Tricks Video URL` AS tips_tricks_video_url,
`Average Overall Rating` AS avg_overall_rating,
`UGC Hashtag` AS hashtag,
`Dilemma Copy` AS dilemma_copy,
CONCAT("0", `UPC`) AS upc
FROM rosie_us_feed;